name: CI Simple

on:
  push:
    branches: [main, dev]

jobs:
  validation:
    runs-on: ubuntu-latest
    
    steps:
    - name: Vérifier le code
      run: |
        echo "✅ Vérification du fichier HTML..."
        echo "📊 Taille : $(wc -l < index.html) lignes"
        
        if grep -q "<!DOCTYPE html>" index.html; then
          echo "✅ Structure HTML correcte"
        else
          echo "❌ Problème de structure HTML"
          exit 1
        fi
        
        echo "🎉 Validation réussie !"