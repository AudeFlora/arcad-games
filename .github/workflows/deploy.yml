name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Checkout du code
        uses: actions/checkout@v4
        
      - name: Setup Pages
        uses: actions/configure-pages@v3
        
      - name: Construire l'application
        run: |
          echo "📁 Vérification de la structure..."
          ls -la
          
          echo ""
          echo "🏗️ Construction depuis src/..."
          
          # Vérifier que src/ existe
          if [ ! -d "src" ]; then
            echo "❌ ERREUR CRITIQUE : Dossier 'src/' introuvable !"
            echo "   Créez un dossier 'src/' et mettez index.html dedans"
            exit 1
          fi
          
          # Vérifier index.html dans src/
          if [ ! -f "src/index.html" ]; then
            echo "❌ ERREUR CRITIQUE : src/index.html introuvable !"
            echo "   Contenu de src/ :"
            ls -la src/
            exit 1
          fi
          
          # Construire dans le dossier par défaut de GitHub Pages
          mkdir -p _site
          cp -r src/* _site/
          
          echo "✅ Construction réussie !"
          echo "📁 Contenu de _site/ :"
          ls -la _site/
          
      - name: Upload l'artefact
        uses: actions/upload-pages-artifact@v2
        with:
          path: '_site'
          
      - name: Déployer sur GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2